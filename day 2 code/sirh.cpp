#include<iostrem>
using namespace std;

int main() {
      // The const keyword specifies that avariable's value is constant    
  // tells the compiler to prevant anything from modifying it
    // (read- only)
const double Height=1000;
  const double Pi=3.141;
} 
